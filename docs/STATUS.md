# orbsim — status

Kind: reference
Binding: no — this file states facts, not rules
Read when: you need a version, a count, or "what is next". Update it whenever
one of those changes.

**Every number in this project that goes stale lives here, and only here.**
Prose elsewhere links to this file rather than quoting it. That is not
tidiness: on 2026-09-09 the assertion count `3,632` appeared 21 times across
10 files, `README.md` still said "about 3,500", `CODING_GUIDELINES.md` still
named clang 22 as the primary compiler and the test harness as hand-rolled,
and `VERIFICATION.md` still named WSL's clang as 21.1.8. Twenty-one copies of
a number are not twenty-one confirmations; they are one fact with twenty
chances to rot — which is
[`VERIFICATION.md`](VERIFICATION.md) rule 23 applied to the documentation.

A *dated* measurement is not a current claim and does not belong here: "the
fuzzer ran 77.4 million executions clean on 2026-09-07" is a fact about that
day and stays in [`HISTORY.md`](HISTORY.md).

Last updated: 2026-09-24.

## Contents

- [Where the project is](#where-the-project-is)
- [Test suites](#test-suites)
- [What this machine has](#what-this-machine-has)
- [Decision records](#decision-records)

---

## Where the project is

**Milestone 0 is complete: a tested two-body core and a Vulkan renderer that
opens a window and paces frames.** Nothing is drawn yet.
[Milestone 1](plan/milestone-1-earth.md) — Earth, orbit track, Orbit MFD — has
**started**.

| | |
|---|---|
| Current milestone | 1 — Earth, orbit track, Orbit MFD |
| Last task completed | [M1-12](plan/tasks/m1-12-render-quality.md), `Count<Derived>`, `Pixels` and the empty `RenderQuality`, 2026-09-24 -- **the renderer can be told what to trade, and the simulation cannot hear it**. A quality value travels from the application into every frame and nothing reads it, which is the whole task: threading a setting through a renderer built for one fixed configuration is a retrofit, and it is nearly free while there is one draw call. `Count<Derived>` is the integral sibling ADR 0001 asked for on 2026-09-08, and **nothing it does wraps round in either tree** -- `assert` alone would have guarded only Debug, where `Texels{1} - Texels{2}` is otherwise a valid constant expression worth 4,294,967,295, so the refusal is a call to a function deliberately not `constexpr`, measured on three implementations to cost exactly the three instructions an unguarded subtraction emits. **`Pixels` has a dimension of its own, not a kind** (decision 137), and that reversal is the task's lesson: a kind permits explicit construction by design, so `Pixels{someRatio}` was always legitimate mp-units, and the agreement of clang and MSVC was an accident of `Scalar<>`'s deleted conversion operator rather than anything a kind promised. **gcc alone said so.** The answer is what mp-units does for the angle, which this project already relies on for `Radians`, and it keeps the algebra: `Pixels / Metres * Metres` is `Pixels`, type-checking M1-50's screen-space error before M1-50 exists. The mutation pass ran three times across two trees and found three things, every one of them in the *pass*: that the guard's two halves mask each other in opposite builds (now `VERIFICATION.md` rule 19's third trap), that a mutant which does not compile is not a kill, and that a pass can hang a machine on a dialog nobody can see. **And the test written to close its declared survivor did not close it** -- the probe reported the fault correctly and returned 1, and the script judging it failed only on exit 0, so it reported green against a tree whose guard was gone. Found by being asked what the survivor was and measuring the answer instead of repeating it. There are three probes now, one per guarded operation, after deleting the guard from `operator+` passed the whole of `check`. Before it, [M1-11](plan/tasks/m1-11-camera.md), the camera and the f64 -> f32 boundary, 2026-09-22 -- **the simulation now has one place where it meets 32-bit arithmetic**, and `grep static_cast<f32> src/` finds three casts in one function. What the task exists for was confirmed and what its document claimed about it was not: the gap between neighbouring 32-bit floats at Earth radius is **exactly 0.5 m**, but narrowing before subtracting costs only **5.8e-4 px** at 400 km -- **86 times inside** the 0.05 px budget the document said it would break "by a wide margin". Half a metre at 400 km is 1.25 microradians. Measured twice before any code was written, in NumPy and again through the project's own types, agreeing to four digits. What camera-relative rendering actually buys is an error **independent of the distance to the origin** -- flat at 5.3e-5 px from Earth radius to 4.5e12 m -- so the suite asserts a **conditioning law**, `2 * (height/2) * 2^-24 * focalLength`, which contains no distance and was measured worst 0.683 of; the naive path's failure is asserted where it is real, at **1 AU** (95 times over) and at **short range** (8.6 times at 1 km, 111 at 100 m); and the 400 km case is **kept and asserted to pass**, so the wrong claim cannot come back. [M1-20](plan/tasks/m1-20-planetary-grid.md) carried the same wrong sentence and was amended the same day (decision 120). **`Camera` validates itself** -- [ADR 0022](adr/0022-a-bounded-scalar-validates-itself.md)'s first composite type, decision 116: a private constructor, a `from()` returning `std::expected`, and `CameraError` naming one failure per argument, still trivially copyable and measured so before the shape was chosen. **Two more of the document's tolerances fell to one measurement** (decision 118): a quaternion of length 1 + e gives a view matrix whose orthonormality residual is about **ten times e**, across six decades, so the orientation tolerance *is* the matrix budget. `kUnitQuaternionTolerance` is **16 ulp**, and it was ruled twice: first at 4, on this project's statement that composing rotations would only drift past it after "a few hundred" products, then at 16 when the suite failed and the real figure turned out to be **five** -- 2.5 ulp after one product, 4.5 after five, 6.0 after ten, because a freshly normalised quaternion is not itself exactly unit. Sixteen gives the ten-product caller the same 2.7 times margin that 4 gave the direct producers. Orthonormality, the determinant and the round trip are therefore **270 ulp**, twice the 135, 135 and 132 measured over 200,000 orientations sitting on that boundary. The document's 1e-15 is unreachable and its 1e-14 is 45 ulp, which the admitted orientation error now exceeds outright. One claim the document did not ask for is in the suite because nothing else would catch the classic error: **the camera's own axes must land on view space's axes**, since a rotation and its inverse are both rotations and round-trip perfectly against each other -- and the mutation pass confirmed that is what kills a missing conjugate, first and alone. The pass was **fifteen mutants, and it ran twice**: 10 caught, 1 survived and 4 invalid first. The survivor was real -- a `bitIdentical` comparing only `x` passed every case in the suite, because every `Vec3f` compared there differs from its expected value in `x` or not at all -- and two assertions now close it. The four invalid ones were wrong in the mutants rather than in the code: `&& false` on a guard makes its `return` unreachable, which `-Wunreachable-code-return` rejects. Second run: **fifteen of fifteen, none surviving, none invalid**, one of them dying at compile time on a `static_assert`. Before it, [M1-87](plan/tasks/m1-87-validated-scalars.md), `Eccentricity` and `GravParam` validated at construction, 2026-09-22 -- **a bad value now has nowhere to live**, and the task existed because a consistency pass over the source found `orbit/` answering one class of bad input three different ways: three public functions reported a non-positive `mu`, two asserted it, and three anomaly converters did neither. The last of those was measured rather than argued -- a probe against the real `orbsim_core` showed a negative eccentricity between -1 and 0 returning a **finite, plausible, wrong** number, with each converter handing back the other's answer for +0.5 because the `sqrt((1-e)/(1+e))` factor inverts. Both scalars are classes with reporting factories now, so `OrbitError::NonPositiveGravity` and its five checks are gone, and three assertions nothing could reach went with them. **`GravParam` kept its quantity** ([ADR 0022](adr/0022-a-bounded-scalar-validates-itself.md), decision 110): `Scalar<R>` derives from `mp_units::quantity`, so a class that holds one keeps the unit algebra and `mu.quantity() / (r * r)` is still an acceleration -- which ADR 0019 promised M1-62 and which `specificEnergy` in the test support already relied on. The mutation pass was **twelve mutants, and it ran twice**: 11 caught and 1 survived first, because the *test* was wrong -- every refusal case read `error()` without asserting `!has_value()` first, which is undefined behaviour that compares equal to the zero enumerator, so it passed while the factory accepted a NaN. The guard had been dropped while splitting the suite to clear a cognitive-complexity finding. Second run: **twelve of twelve, none surviving, none invalid**, seven at compile time. Before it, [M1-10](plan/tasks/m1-10-reverse-z-projection.md), reverse-Z with an infinite far plane, 2026-09-21 -- **[ADR 0003](adr/0003-reverse-z-depth.md) is real**, seventeen days after it was accepted, and its central claim is now a measurement rather than an argument: two points a metre apart at 1000 km land **9 to 14 ulp apart** in 32-bit depth, where a conventional projection with a far plane at 1e9 m puts them on **bit-identical** values. The control is in the suite beside the claim. The depth row is constant, so `depth == nearPlane / distance` exactly, and that rule is asserted bit for bit rather than one of its consequences. **`constexpr` did not survive the three front ends** (decision 99): clang and MSVC refuse `std::tan` in a constant expression and gcc-14 accepts it, so the entry point is `inline` and a `constexpr` helper carries the layout in seven `static_assert`s -- which is why six of the pass's fourteen mutants die before a test runs. Four of the task's numbers were replaced by measurement (decisions 99-104), and the flat projection for the instrument panels was ruled **out** after its consequences were measured (decision 105). The mutation pass was **fourteen mutants, fourteen caught, none surviving, none invalid** -- it killed M1-09's declared survivor and **corrected two written-down claims that had never been run**: M1-09's prediction about which test would catch that survivor, and this task's own comment about what its extra assertions buy. Before it, [M1-09](plan/tasks/m1-09-orbsim-view.md), the `orbsim_view` library and `Mat4`, 2026-09-20 -- **the first render-side code, and the tests can link it**. `orbsim_view` links `orbsim_core` and nothing else, so "nothing under `tests/` includes a Vulkan or SDL header" is now a property of the link graph ([ADR 0012](adr/0012-orbsim-view.md)). `Mat4` carries the units of **both** spaces it maps between -- [ADR 0020](adr/0020-transforms-carry-their-units.md), the twentieth record -- so `projection * view` compiles and `view * projection` does not, and `transformPoint` refuses a projection by type. Two of the task's three tolerances did not survive measurement and became laws instead: associativity against the conditioning `2*gamma_8`, and the rigid inverse split into a dimensionless rotation block and 20 ulp of \|t\|, because the translation column is in **metres** and 1e-14 there is only true below about 5 m. **Frames followed the same day** at the owner's request ([ADR 0021](adr/0021-transforms-carry-their-frames.md), reversing decision 94): `view * projection` was already refused, and now so are `model * view` in the wrong order and a point handed to a matrix that does not start in its frame. The transpose became a **dual** map on the owner's suggestion, which is what makes its law hold for a chain containing a projection rather than only for affine matrices. Measured before deciding: no runtime cost at all -- 35 instructions against 35, identical. The mutation pass was **fifteen mutants, fourteen caught, one declared survivor, none invalid** -- seven killed by a `static_assert` before a test ran, the survivor handed to M1-10 by name, and one of the three frame mutants finding a hole in a *test* rather than in the code: `retargetFrame` had been checked by retargeting back, which cancels any fault applied twice. Before it, [M1-08](plan/tasks/m1-08-solar-position.md), the solar position, 2026-09-20 -- **phase A's astronomy is complete**. The geocentric Sun from ERFA's `eraEpv00`, negated and geometric, within **0.02″ of direction and 5e-8 AU of distance** of JPL Horizons over 2000-2050 -- measured worst 0.0085″ and 2.14e-8 AU, so both budgets are about twice the measurement rather than sixfold headroom over ERFA's own claim. `Irradiance` joins `core/Units.hpp` and the solar constant carries Kopp & Lean (2011). Nine questions went up before any code and were ruled the same day (decisions 85-91), one of them reversing a first instruction: `TwoPartDate` was **deleted** rather than lifted, because it was a field-for-field copy of `core/Time.hpp`'s `JulianDate`. Before it, [M1-07](plan/tasks/m1-07-earth-orientation.md), precession, nutation and the Earth rotation angle, 2026-09-20 -- **the Earth has an orientation**, from ERFA's `eraC2t06a`, within **0.1 mas** of Skyfield over 1900-2100 (measured worst 53.5 µas, of which 47 µas is the TIO locator s'). A TT-only rotation comes with it for M1-08 and M1-63, and the matrix-to-quaternion conversion is `core/Math.hpp`'s, with a suite of its own because the Earth's matrices reach only two of its four branches. Before it, [M1-86](plan/tasks/m1-86-ut1-from-tt.md), UT1 from TT, 2026-09-20 -- **the Earth's clock no longer needs the leap-second table**. UT1 = TT - DeltaT, with DeltaT a model the caller names: exact where the table gives it, a named constant past its expiry, and a stated drift instead of a refusal. Split out of M1-07 that day, with its own three rulings (decisions 72-74). Before it, [M1-05](plan/tasks/m1-05-tdb-and-ut1.md), TDB and UT1, and ERFA pinned, 2026-09-19 -- **all five scales convert**. Its fifteen questions were put to the owner before any code and ruled the same day: decisions 53-67 of the [register](plan/milestone-1-decisions.md). Before it, [M1-06](plan/tasks/m1-06-horizons-fixtures.md), the Horizons fixture format, and [M1-04](plan/tasks/m1-04-leap-seconds.md), UTC, TAI and TT |
| Before M1-04 | **All done, 2026-09-13.** Two tasks on 2026-09-12: `elementsFromState` is now accurate to the resolution of a double -- every element within 40 u of the nearest double to the exact conversion, worst case 9.4 u, measured against 60-digit references over 56,532 states across nine families on three toolchains. The cancelling steps are carried in double-double ([`src/core/DoubleDouble.hpp`](../src/core/DoubleDouble.hpp)) on a state scaled by a power of two. And the circular threshold moved from e = 1e-9 to 1e-15, where the 2e it costs a round trip is the resolution of a double rather than 1.8e-9. Then two more on 2026-09-13: the five findings that commit left open, and **`stateFromElements` reports** ([ADR 0018](adr/0018-state-from-elements-reports.md)) -- it returns `std::expected` and a new `UnreachableAnomaly`, after it was found returning a NaN position for 169 of 10,000 nearly radial element sets and a radius 2.2 times too small, unmarked |
| Next task | [M1-13](plan/tasks/m1-13-pipelines.md), the graphics pipelines. Its prerequisites are M1-09, M1-10 and M1-12, all done, so nothing blocks it. It is also where `projectionOf` in `view/Camera.hpp` is deleted, which M1-11 put in on the understanding that this task removes it (decision 117), and where `FrameContext::quality` becomes a parameter rather than a field somebody must remember to fill, if it should (decision 127) |
| Still open, for whoever reaches it | **`CountError` in `core/Scalar.hpp` has no `describe()`**, and it is the one error enum in the project without one (decision 141). A `switch` over a single enumerator is what `readability-trivial-switch` reports; decision 91 ruled that exact case for `describe(EphemerisError)` with a `NOLINT` at the site, which is the owner's to grant and was not asked for here. Nothing is lost while the enum has one value -- the name says what the failure is and no caller wants text -- and whoever adds the second enumerator writes `describe()` then, by which time the switch is not trivial. **The decision to make is whether to grant the suppression now or wait.** Before it, the `Bytes` question (closed by decision 140) and `Scalar<>`'s deleted conversion operator (closed by 138) |
| Closed earlier | **Nothing else stands open.** The `perspectiveDivide` finding that sat here since 2026-09-21 was closed on 2026-09-22 (decision 122), and measuring it found the recorded half was the smaller one: a zero denominator is loud -- infinity, or a not-a-number -- while a **negative** one is silent and puts a point behind the camera back on screen, mirrored, with x and y **bit-identical** to a point in front. The zero is now a precondition of `perspectiveDivide`; the sign is `isInFrontOfCamera` on the clip-space type, with a regression case asserting the mirror |
| Then | `orbsim_view`, the camera and the f64 → f32 boundary, the pipelines, the HDR target and the AgX tonemap, and the probe mode that verifies everything drawn after it. **The astronomy of phase A is finished**: the five time scales, the Earth's orientation and the Sun all exist and are asserted against data this project did not produce |
| Reference data | **The Earth's orientation at 1,029 epochs, 1900-2100**, from Skyfield 1.55 -- the celestial-to-terrestrial rotation, committed as [`data/skyfield/earth-orientation.txt`](../data/skyfield/earth-orientation.txt), which M1-07 asserts 0.1 mas against. **The geocentric Sun at 40 epochs, 2000-2050**, from JPL Horizons (DE441), generated into gitignored `data/horizons/` and converted by `scripts/horizons-fixture.py` -- **asserted against since M1-08**, at 0.02″ and 5e-8 AU, which is the first budget this project has claimed against Horizons itself. SHA-256 `61291048...7fd53`, committed in [`data/horizons/checksums.sha256`](../data/horizons/checksums.sha256) and verified by `check`. **A fresh clone has none**: run the recipe in [`data/horizons/README.md`](../data/horizons/README.md), or the suites that need it report themselves skipped. **And TDB - TT at 1,975 epochs, 1900-2100**, from Skyfield 1.55, **committed** as [`data/skyfield/tdb-minus-tt.txt`](../data/skyfield/tdb-minus-tt.txt) -- SHA-256 `e80fdf8b...7dc171` when generated -- so every clone checks against it |
| Leap-second table valid until | **2027-01-01T00:00:00 UTC**, from IERS **Bulletin C 72** (Paris, 2026-07-06), which rules out a leap second at the end of December 2026 and says nothing later. Past it, every UTC conversion reports `LeapSecondTableExpired` by name rather than extrapolating. Renewing it is four lines in [`src/core/LeapSeconds.hpp`](../src/core/LeapSeconds.hpp), which says how |
| Phase order | A → B → D → C → E → F → G |
| Working branch | `master`. **Everything through M1-12 and its five follow-on decisions is pushed**, 2026-09-24: `howManyFit` reporting rather than returning nothing useful (decision 141), the byte count and the representation `Count` gained to carry it (140), the conversion operator narrowed (138), the harness taught to judge a probe (139), the count type and the pixel dimension, the empty `RenderQuality` and the path it travels, the three wraparound probes, the abort listener and the harness timeouts, the conversion operator narrowed, and the records -- nine commits. Verified by fetching and comparing `master` against `origin/master` rather than assumed, which is how every entry in this row has been checked since 2026-09-20. Before them, everything through M1-11, 2026-09-22: the `core` predicate, the camera and its suite, the records, the correction the mutation pass forced and the pass's result, the perspective divide's guard, and the six-toolchain run. Verified by fetching and comparing `master` against `origin/master` rather than assumed, which is how every entry in this row has been checked since 2026-09-20. M1-10's four commits are in there too -- the projection, its mutation pass and the correction that pass forced, the records, and the four-toolchain re-run. Verified by fetching and comparing `master` against `origin/master` rather than assumed, which is how every entry in this row has been checked since 2026-09-20. **The commit hash is deliberately not written here**: this row named `bac8b0b` until 2026-09-21, and it had stopped being true the moment the commit that wrote it existed -- a self-referential claim that goes stale on the way in. Run `git fetch && git status -sb` for the answer instead; `git fetch && git switch master` is all another machine needs. **No stale branches.** The five merged ones -- `clang-23-2026-09`, `consistency-fixes-2026-09-13`, `docs-reorg`, `pre-docs-reorg-2026-09-09` and `review-fixes-2026-09` -- were deleted on 2026-09-22, local and remote, each verified as carrying no commit `master` does not have immediately before the delete. `master` is the only branch on either side now |

| Component | State |
|---|---|
| `src/core/` | Scalars and vectors that carry their unit over mp-units, Quat, a `RotationMatrix` and the conversion from one to the other, and `TimePoint` on five time scales. **UTC, TAI and TT convert** in exact integer picoseconds over the IERS leap-second table in `core/LeapSeconds.hpp`, and **UT1 converts from UTC and from TT**, exactly: from UTC with a validated `DeltaUt1`, and from TT with a validated `DeltaT` the caller names (M1-86), which is what keeps the Earth's orientation off the leap-second table. Both model errors are stated where they are taken -- 0.9 s, about 420 m at the equator, and past the table's expiry the drift of a held DeltaT, at worst 1.15 s a year on record. TDB is `src/astro/`'s. Complete for what exists. **`isUnitQuaternion` and `kUnitQuaternionTolerance` joined with M1-11**, beside `isRotation` and `kRotationTolerance`: 16 ulp of unit length, set from what composing rotations costs rather than from what one producer costs, and still four orders tighter than the matrix tolerance because a quaternion of length 1 + e yields a matrix about ten times e from orthonormal. `bitsOf` gained an `f32` overload for the one type this project keeps in 32 bits. |
| `src/astro/` | **Begun with M1-05, the Earth turns since M1-07, and the Sun has a place since M1-08.** The geocentric Sun through `eraEpv00`, geometric and in ICRF, within 0.02″ and 5e-8 AU of Horizons over 2000-2050, reporting `OutsideEphemerisRange` past ERFA's span; `solarIrradianceAt` is the inverse-square law from 1361 W·m⁻². TT <-> TDB through ERFA's `eraDtdb` at the geocentre, within 20 µs of an independent reference -- 7.89 µs measured -- and back to within a picosecond. **The celestial-to-terrestrial rotation** through `eraC2t06a`, the celestial-intermediate one through `eraC2i06a` and the Earth rotation angle through `eraEra00`, within **0.1 mas** of Skyfield over 1900-2100 -- 53.5 µas measured -- with polar motion and a measured DeltaUT1 omitted and their sizes in the header. ERFA v2.0.1 is compiled from source and linked privately, and its headers are included here and nowhere else. |
| `src/orbit/` | Two-body: state↔elements, universal-variable and element propagation, anomaly conversions. Correct at every scale from lunar to outer-solar-system. |
| `src/view/` | **New with M1-09, and since M1-10 it projects.** `view/Projection.hpp` holds the reversed perspective with no far plane, an `Aspect` kind of its own, and a `ProjectionError` reporting a near plane, an aspect ratio or a field of view a caller can actually produce. The clip conventions are stated there once -- right-handed view space down -z, Vulkan's y downward, depth reversed -- and the y flip is one negated entry rather than a sign in the shaders. The render-side maths Vulkan never touches, as `orbsim_view` -- which links `orbsim_core` and nothing else, and which `orbsim_core` cannot link back (a configure-time assertion says so). `Mat4<kInXyz, kInW, kOutXyz, kOutW>` and its `Vec4`, column-major, every block reference derived: `Transform` is metre-to-metre affine, `Projection` maps a world point to clip space where w is metres, and the divide lands in dimensionless normalised device coordinates. **And since ADR 0021 the frames too**: `Mat4<kFrom, kTo, ...>` with a `FrameTag` that carries a dual flag, a view-local `FramedVec3` so `core` stays frame-free, same-frame factories, and `retargetFrame` as the one declared frame change -- `grep retargetFrame` is the audit. Header-only, so the library is INTERFACE until M1-11 adds `Camera.cpp`. **And since M1-11 it has a camera, and a library.** `view/Camera.hpp` holds the validated `Camera` -- a private constructor and a `from()` returning `std::expected<Camera, CameraError>`, ADR 0022's first composite type -- the rotation-only `viewMatrix` that is the one caller of `retargetFrame` in `src/`, and `toRenderSpace`, **the only place in `src/` that narrows to 32 bits**: three `static_cast<f32>` in one function, after a subtraction done in 64. `projectionOf` sits beside them and is **temporary by decision**, deleted when M1-13 lands. `orbsim_view` is STATIC now that `Camera.cpp` exists; it still links `orbsim_core` and nothing else. |
| `src/render/` | Vulkan 1.3 device, swapchain, frame pacing, RAII handles, buffer upload, shader loading. **No pipelines, no drawing.** |
| `src/app/` | Window, event loop, argument parsing, frame loop. |
| `shaders/` | Four GLSL shaders compile to SPIR-V at build time and are **never loaded**. They are placeholders for phase A. |
| `tests/` | **`tests/AbortBehaviour.cpp` compiles into every suite since 2026-09-24** -- a Catch2 listener that stops the Windows debug runtime turning a failed assertion into a modal dialog. Nothing in a healthy build reaches it; a *mutation pass* makes the code wrong on purpose, and two of M1-12's mutants hung the machine twice in one run before it existed. Compiled into each executable rather than linked from `orbsim_test_support`, because a listener in a static library is dropped unless something references it. Fourteen Catch2 suites, 1,500,523 assertions in 202 test cases, plus ERFA's two validation programs, a GPU smoke test, two fixture tests, three count-wraparound probes and two libFuzzer targets. *(This row said eleven suites and 1,369,392 in 155 until 2026-09-22, and 1,496,813 in 184 until 2026-09-24 -- each time the Totals paragraph below had been updated and this row had not. Twice now, in the file whose opening note is about exactly that. The two are worth reading together: the row is the one that keeps going stale, because the total is what a task re-derives and the row is what nobody thinks to look at.)* |

## Test suites

| Suite | Assertions | What it covers |
|---|---|---|
| `test_astro_time` | 42,143, in 5 cases | `astro/Tdb.hpp`, **added with M1-05**: TDB - TT at all 1,975 epochs of the committed Skyfield fixture within the 20 µs budget -- 7.89 µs worst -- and not vacuously, since the full series and the seven-term one genuinely differ; the annual term's peaks within 60 µs of the Kepler problem's 2e√(GM a)/c² with JPL's mean elements, and its four zero crossings in 2024-2025 within 2.1 days of USNO's mean anomaly at 0° and 180°, upward at perihelion; the change over twelve hours of one day matching the annual term's to 1 µs, every fifth day of 2024-2025 -- which the fixture's midnights cannot check; TT → TDB → TT and TDB → TT → TDB within a picosecond over a seeded sweep of 1990-2050, and exact on at least 99% of it, which is what shows the series is evaluated at TDB; and both ends of the calendar converted, not refused |
| `t_erfa_c`, `t_erfa_c_extra` | 1,494 checks, and the version and leap-second functions | ERFA's own validation programs, **added with M1-05**, run as upstream's `make check` runs them. They prove the library was built right by this toolchain with these flags, and each exits non-zero on any failure |
| `test_earth_orientation` | 32,087, in 8 cases | `astro/EarthOrientation.hpp`, **added with M1-07**: the Earth rotation angle at J2000.0 against the value IAU 2000 Resolution B1.8 defines, to 2e-13 rad -- what ERFA's own arithmetic reaches with this date split; one turn of it taking the stellar day, 86 164.098 903 691 s, recovered from the angle to 1e-7 s at four epochs; the whole rotation within **0.1 mas** of Skyfield at all 1,029 fixture epochs, worst 53.5 µas; the composition CIO-consistent, the test named after the error the plan carried, with the leftover spin under 1 mas -- 47 µas, which is s'; only UT1 turning the Earth, the pole unmoved and a vector in the equator turned by exactly the change in ERA, to 4e-15; the pole of date bit-identical with and without the Earth's turn; the intermediate frame being the full rotation less Rz(ERA), to 0.1 mas, which is what stops a wrapper returning the whole rotation from passing; and the quaternion a unit one to 1e-15, reproducing ERFA's matrix to 2e-15 over 2,000 epochs, half of them within a milliradian of a half turn |
| `test_math` | 88,236, in 6 cases | `core/Math.hpp`'s matrix-to-quaternion conversion, **added with M1-07** -- the first suite that header has had. The identity and the three half turns exactly; every one of the four branches reached over a seeded sweep of 20,000 rotations, each reproducing its matrix to 4e-15 with a unit norm and the largest component positive; within 1e-7 rad of a half turn, where the trace-only conversion is off by more than 1e-6 and this one holds; and `isRotation` accepting what rounding produces, refusing a scaled matrix, a reflection and zeros, and -- deliberately -- accepting a transposed rotation, which only the reference test can see **Two cases joined with M1-11**: `isUnitQuaternion` refusing what was never a rotation -- the zero quaternion, a scaled one, a NaN, an infinity -- and accepting everything this project builds, which is the half that says a 4 ulp tolerance is usable rather than merely strict |
| `test_double_double` | 135,020, in 8 cases | `core/DoubleDouble.hpp`: the exact product against `std::fma`, which computes the same rounding error by a completely different route, over 20,000 operands spanning every scale; the exact sum against `std::int64_t` arithmetic; the split above its 2^996 ceiling, where it used to return a correct product with a NaN error term; a cancelling difference keeping what a double loses; the quotient and the square root reconstructing their inputs to 2^-100; and every way an overflow could become a NaN instead of staying an overflow |
| `test_fixture_file` | 27,387, in 17 cases -- **27,261 with one case skipped** where the Horizons fixture has not been generated | `tests/FixtureFile.hpp`, the reference-data reader: a known-good file; comments, blank lines and CRLF; fifteen malformed files, each refused by name **and line**; a missing file; every error describing itself; 10,000 seeded doubles written with 17 digits reading back bit for bit; kilometres to metres against exact rational arithmetic on three values where parsing and then multiplying by 1000 gives the wrong double; state vectors arriving as `TdbTime`, metres and m/s against the compiler's own literals; a fractional epoch kept exact; the wrong frame, scale, corrections or units refused; and the generated Sun fixture itself -- 40 rows, J2000.0 first, every distance in metres between 0.98 and 1.02 AU. **M1-05 added three cases**: a TDB - TT fixture read as `TdbTime` and seconds, bit for bit against the compiler's literals; the wrong scale or unit refused by name and line; and the committed Skyfield fixture itself -- 1,975 rows 37 days apart from 1900-01-01, every value in seconds. **M1-07 added three more**: an Earth-orientation fixture read as two instants and a rotation, against the compiler's own literals; five ways of getting its header wrong refused by name and line, including polar motion applied and the frames the other way round; and the committed orientation fixture itself -- 1,029 rows, every one a rotation, each UT1 exactly its TT less the DeltaT the header names, and every row at a different time of day |
| `count_wraparound_{subtraction,addition,multiplication}_aborts` | -- | `core/Scalar.hpp`'s wraparound guard at **run time**, **added with M1-12**. **Three of them**, one per guarded operation, because a program can only abort once -- and because there was one for a day, covering the subtraction only, and deleting the run-time guard from `operator+` then passed the whole of `check`. A program of its own rather than a Catch2 case, because the claim is that an assertion fires and an assertion that fires calls abort -- which takes the reporter, the counts and every case that had not run yet with it. **Its failure is the pass**: `cmake/VerifyCountWraparound.cmake` fails if the probe returns normally, and reports **SKIPPED** where assertions are not live, because under `NDEBUG` the run-time half of the guard genuinely does not exist and a test that passed there would be reporting a check it had not made. So it passes in `debug` and skips in `relwithdebinfo`. It exists because M1-12's mutation pass declared a survivor: deleting that assertion changed nothing any test could see (decision 136) |
| `horizons_fixture_checksums` | -- | The generated Horizons fixtures against their committed SHA-256 sums, by a CMake script. A wrong hash fails and names both; a fixture not generated is reported **skipped** |
| `horizons_fixture_converter` | -- | `scripts/horizons-fixture.py --self-test`: a synthetic response to a known text; identical output when the timestamp and Earth-orientation lines change; eleven refusals. Its input holds 2^53 + 1, which no double can, so a converter that re-printed the numbers fails |
| `test_sun` | 83,489, in 8 cases | `astro/Sun.hpp`, **added with M1-08**: the geocentric Sun within **0.02″ and 5e-8 AU** of JPL Horizons at all 40 fixture epochs -- worst 0.0085″ and 2.14e-8 AU -- and not vacuously, since a distance agreeing to better than 1e-11 AU would mean the fixture had come out of ERFA rather than out of Horizons; the distance bit-identical to the length of the position, which is what makes one series evaluation a structural claim rather than a hope; the apsides swept daily over all fifty years in windows holding one apsis each, within 1e-4 AU of the published 0.9833 and 1.0167 and on 2-5 January and 3-6 July -- **the calendar year is the wrong window**, and putting the "annual minimum" on 31 December in 2003 and 2047 is how that was found; the daily angular step inside a two-sided 0.952°-1.020°, derived from two-body motion at e = 0.016709 plus the Earth's wobble about the Earth-Moon barycentre; the March equinox at three USNO instants, the declination within 0.01° of zero **and within 1e-3° of the +0.002265° the omitted aberration predicts**, so the residual is claimed rather than merely bounded; the inverse-square law exact at 1, 0.5 and 2 AU, also as `static_assert`s; and the span by name at both ends, a millisecond either side, from both functions, with mid-2100 outside |
| `test_units_validated` | 67, in 13 cases | `core/Units.hpp`'s two validated scalars, **added with M1-87**. Almost everything these types buy is a compile-time refusal and lives as a `static_assert` beside them; this suite holds the two things one cannot. **NaN and both infinities**, because MSVC's constant evaluator disagrees with its own runtime about NaN, so an assertion about one is a claim about the evaluator. And **the refusals that used to live in `orbit/`** -- a massless central body, a negative mu, a NaN mu -- moved down to the factory that now does the refusing, since the calls that used to test them no longer compile. Every refusal asserts `!has_value()` **before** it reads `error()`, which is not decoration: reading `error()` on an expected that holds a value is undefined behaviour and compares equal to the zero enumerator, so a case without the guard passes while the factory accepts what it should refuse. The mutation pass found exactly that |
| `test_view_math` | 84,026, in 10 cases | `view/Mat4.hpp` and `view/Frame.hpp`, **added with M1-09**, and the first suite that links `orbsim_view`. Most of what the frames buy is not here at all, because it is a compile-time refusal: those are `static_assert`s beside the functions they constrain, each inverted and watched failing. What this suite holds is the arithmetic: associativity against the conditioning `\|A\|\|B\|\|C\|` at `2*gamma_8` = 1.78e-15, measured 6.54e-16, with a guard that the two groupings genuinely differ so the case cannot pass vacuously; the identity and `transpose(transpose(M))` **bit-identical**, and `transpose(AB)` bit-identical to `transpose(B)transpose(A)`, which is the row/column-major check and is exact because both sides sum the same four products in the same order; the rigid inverse split in two, 1e-14 on the dimensionless rotation block and 20 ulp of \|t\| on the translation, at 1 m, Earth radius, lunar distance and 1 AU; a point through the transform and back within 24 ulp of the scale; a translation moving a point and leaving a direction alone, which is the whole reason the two have different names; a rotation agreeing with the quaternion it was built from; `isAffine` accepting the identity and a translation and refusing a bent bottom row and a corner one ulp from one; and `retargetFrame` leaving all sixteen elements alone, compared element by element rather than by retargeting back -- which is what the mutation pass caught, since applying a faulty operation twice cancels its fault |
| `test_projection` | 21,651, in 11 cases | `view/Projection.hpp`, **added with M1-10**, and the second suite to link `orbsim_view`. The matrix's *layout* is not here at all -- it is seven `static_assert`s beside the builder, which is what kills six of the task's fourteen mutants before a test runs. What this suite holds: the near plane at depth exactly 1.0 at five near planes, **none of them 1 m**, with the depth numerator and denominator asserted separately from the quotient, because at 1 m dividing and multiplying agree and the case would accept M1-09's surviving mutant; `depth == nearPlane / distance` **bit for bit** over five near planes and seven distances out to 1e20 m, which is the rule rather than one of its consequences, with the plan's "under 1e-12 at 1e13 m" kept beside it at a near plane where it is true; ten times the near plane giving 0.1 to 2 ulp, measured 1; depth strictly decreasing over a fixed grid of 1,500 points from 0.1 m to 1e13 m, ending under 1e-13; **the precision claim ADR 0003 rests on, with its control** -- two points a metre apart at 1000 km separating by at least 4 f32 ulp, measured 9 to 14, against a conventional projection with a far plane at 1e9 m that puts them **bit-identical**, plus the all-f32 path the GPU itself computes, and a non-vacuity case showing the control does separate them closer in; the frustum edge landing on ±1 to 4 ulp, twice over -- at two anchors whose half-angle tangent comes from `std::numbers` rather than `std::tan`, so a conversion error cannot cancel, and over a seeded 20,000-case sweep of field of view, aspect and near plane; the y flip asserted **signed**, a point above the axis landing at -1; and every refusal by name, including an **infinite** near plane and aspect, which a positivity test alone would accept |
| `test_camera` | 123,343, in 13 cases | `view/Camera.hpp`, **added with M1-11**, and the third suite to link `orbsim_view`. The jitter budget three ways: against a **conditioning law** with no distance in it -- `2 * (height/2) * 2^-24 * focalLength`, measured worst 0.683 of -- against phase A's 0.05 px at 1920x1080, and with the whole chain computed in 32 bits as the graphics card computes it. Flatness across six decades of world magnitude, asserted at 5 % and measured 0.06 %. The naive path asserted to **fail** at 1 AU and at short range and to **pass** at 400 km, which is the measurement the task document got wrong. The view matrix orthonormal, unit-determinant and round-tripping to 80 ulp over 20,000 orientations placed on the factory's tolerance boundary, with its translation **bit-identical** to zero. The camera's own axes landing on view space's axes, which is the only case here that can tell a view matrix from its inverse. And the narrowing's exactness: a camera at 6771000.3 m and a point 0.1 m beyond it, where subtracting first gives 0.1 m and narrowing first gives **exactly zero** |
| `test_render_quality` | 3,710, in 18 cases | `core/Scalar.hpp`'s `Count<Derived, Rep>`, `core/Units.hpp`'s `Pixels` and `view/RenderQuality.hpp`, **added with M1-12**, and the fourth suite to link `orbsim_view`. **It grew with decisions 140 and 141**: a byte count on a 64-bit representation, whose probe table crosses 2^32 because that is the range a 32-bit count cannot reach; `toBytes` at 4,096 MiB, which a 32-bit multiplication makes zero; alignment as three separate claims -- it lands on a multiple, never below what was asked, never more than one step above -- and `howManyFit` reporting a block size of zero **by name**, with `has_value()` asserted before `error()` is read. **The smallest suite here for its subject, on purpose**: nearly everything these types buy is a refusal, and a refusal is a `static_assert` beside the type rather than a case here -- including the one that matters, that a count which would wrap is refused *while the compiler evaluates it*, so `Texels{1} - Texels{2}` fails the build rather than producing 4,294,967,295. That assertion has a **positive control** next to it, because "not a constant expression" and "the proof is broken" are otherwise the same answer. What is here is the arithmetic, each result checked against the same operation written on the bare number: the round trip through addition and subtraction, the difference, ordering and equality, the product both ways round, the compound forms, and the ends of the range, which are the values a guard written one comparison too eagerly would refuse -- and the Debug tree's run of them under live assertions is the half that says the guard does not fire where it must not. Plus the same for `Pixels` against plain doubles. **`RenderQuality` has no run-time case at all** and the suite says why: it has no fields until M1-46, so every claim it can make is a compile-time one, and it deliberately has no `==` to compare two presets with |
| `test_orbit` | 759, in 8 cases | Earth-orbit round trips, degenerate orbits, analytic values, propagator agreement, invariants, hyperbolic, Kepler solver, reported failures |
| `test_orbit_scales` | 85,672, in 30 cases | Heliocentric circles, parabolic trajectories, non-finite inputs, states that are finite but are not orbits, states with no orbital plane, `length()` exact at every binary scale, the fuzzer's nearly radial hyperbola at 1e-158 m, nearly radial ellipses and hyperbolas at 7000 km classified by their energy, an eccentricity that rounds to 1, `orbitInfo`'s radius and speed on four states that broke them and a fuzzer state whose semi-major axis underflows, a zero time step on every conic, near-rectilinear orbits, propagation composing, canonical scale invariance, bit-identical determinism, a seeded sweep of 200 closed + 100 hyperbolic orbits around the Moon, Earth, Jupiter and the Sun, a second seeded sweep of 10,000 states -- ordinary, nearly radial, near-parabolic, e down to 1e-16, and out along a hyperbola's asymptote to r/|a| = 1e6 -- against the conditioning of the round trip through the elements, element propagation on five conics within 2e-9 of a parabola against 60-digit references, a third seeded sweep of 2,000 element sets -- ordinary, near-parabolic, parabolic and near-circular -- against the state propagator and against stepping back, seven measured states, one per failure mechanism, where every one of the seven elements is checked against a 60-digit reference, the perifocal velocity's e - 1 at 1 - e = 1e-6, a radial trajectory at the rectilinear threshold from the correct side, a committed checksum pinning the elements that are bit-identical on every toolchain, and three cases on `stateFromElements` -- a NaN position, a radius 2.2 times too small, and an anomaly past a hyperbola's asymptote |
| `test_time` | 772,933, in 47 cases | The published epochs both ways; the calendar against `std::chrono`'s on every day of 1900–2100, and which dates exist against its `ok()`; a seeded calendar round trip; 1 ns at the end of a day and across it, and a million 1 µs steps; arithmetic by the nearest picosecond across every day boundary; ordering against the difference; every refusal by name; the Julian-date conversions against exact rational arithmetic; and, at compile time, that no scale converts to another and UTC and UT1 have no duration arithmetic. **M1-04 added eleven cases:** the 28 published steps against a second transcription and against `std::chrono::get_leap_second_info` on every one of the 20,089 days of the era; the worked example 2017-01-01T00:00:00 UTC = 00:00:37 TAI = 00:01:09.184 TT; TT − TAI exact at 1,000 epochs; every step one second either side, both directions; 23:59:60 representable and round-tripping; a seeded sweep of 10,000 instants plus eight offsets at each of 27 boundaries, all **bit-identical** through UTC → TAI → UTC and through TT; both scales strictly increasing across every boundary; each refusal by name; a **synthetic** negative leap second, which no published table can reach; and the quasi-Julian date of a leap-second day against exactly-rounded reference values. **One case added 2026-09-20**, named after the symptom the fuzzer found: a TAI instant outside the calendar is reported rather than asserted -- the first 32.184 s of year 1, whose TAI falls in year 0, and an instant arithmetic carried past 9999, including a day past what an int64 holds. **M1-86 added eight, on UT1 from TT**: the table's DeltaT against 32.184 s + DeltaAT - DeltaUT1 either side of all 28 steps, for three DeltaUT1; UT1 from it identical to the UTC road's UT1 over a seeded sweep of 1972-2026 and through every leap second a quarter second at a time; the sign and the carries, including a DeltaT of days and one at the 10^6 s limit; both round trips bit-identical with DeltaT drawn to the picosecond; a held DeltaT turning UT1 by exactly each TT step through the leap second of 2016, where the UTC road steps back once; every refusal by name, with 2^17 + 2^-35 s kept to the picosecond, which scaling the whole value would lose; the table's two edges a picosecond either side; and the held constant against the last published step **M1-05 added seven cases, on UT1:** with DeltaUT1 unmodelled, UT1 carries UTC's day and time over the seeded sweep; UT1 = UTC + DeltaUT1 to the picosecond, across midnight both ways; a leap second, where UT1 runs on into the next day and the way back lands a second later; UT1 → UTC → UT1 exact over 10,000 instants with DeltaUT1 drawn to the picosecond; every refusal of a DeltaUT1 by name, and 0.9 s itself accepted; the calendar's two ends; and a **synthetic** negative leap second, whose removed second of UT1 has no UTC |
| `fuzz_orbit`, `fuzz_time` | — | libFuzzer over the core under ASan and UBSan. `fuzz_time` joined on 2026-09-18 with M1-04 and throws arbitrary bytes at the calendar, the Julian date and the scale conversions, asserting that a success is normalised, that a Julian fraction stays in [0, 1), and that UTC → TAI → UTC is bit-identical. M1-05 added a DeltaUT1 to its input and three claims: an accepted DeltaUT1 is inside 0.9 s, UT1 → UTC → UT1 is bit-identical, and TT ↔ TDB round-trips within a picosecond at any date from year 1 to 9999. **M1-86 added a DeltaT and three more** -- an accepted DeltaT is inside 10^6 s, TT ↔ UT1 round-trips bit-identically under any of them, and UT1 from the table's DeltaT is the UTC road's UT1 -- and the first of those runs found the `utcFromTai` precondition that 2026-09-20's fix turned into a report. Not CTest tests: run deliberately with a time budget, `cmake --preset windows-fuzz`. The `linux-fuzz` preset builds the same targets under WSL and is kept only for LeakSanitizer, which Windows has no equivalent of. |
| `orbsim_smoke` | — | Runs the app under the Vulkan validation layers for 2 s; fails on any validation error. Labelled `gpu`. |

*(`test_orbit` and `test_orbit_scales` each lost assertions on 2026-09-22, four and one: the five that could no longer be written once M1-87 made a non-positive mu unrepresentable. Their cases are intact and keep their names; `test_units_validated` carries what they checked.)*

Totals: **1,500,523 assertions in 202 test cases**, summed from the suites
themselves rather than carried forward -- every one was run and its figure read,
because a total nobody re-derives is a number with one chance to rot. *(Which
earned itself again on 2026-09-24: the figure written here from arithmetic
before the suites were run was 1,503,048, and the measurement said otherwise.)*
**The change from M1-12's 1,499,946 is +577 in +8 cases, and all of it is
`test_render_quality`** gaining the byte-count arithmetic of decisions 140 and
141 -- the suite itself went 3,133 to 3,710, which is the same +577; every other suite read back
what it read back before. That the difference is one suite's is the check --
it is what says nothing else moved while `core/Scalar.hpp` was reshaped
underneath thirteen other suites.

**All six toolchains re-run against decisions 140 and 141 on 2026-09-24**,
which reshaped `Count` for every type built on it, and they agree: 210 CTest
tests under `relwithdebinfo`, `debug`, `asan` and `windows-msvc`, and 209 under
`linux-sanitize` and `linux-gcc`, which are core only and so do not build the
GPU smoke test. 0 failed anywhere, and no report from AddressSanitizer or
UndefinedBehaviorSanitizer. *(Decisions 138 and 139 had their own six-toolchain
run earlier the same day, at 202 and 201, and M1-12 itself one before that.)*

**The mutation pass is 28 of 28, with no survivors**, across two trees: 26 in
`build/debug` and 2 in `build/relwithdebinfo`, none invalid and none hung. It
took six runs to get there and **every correction was in the pass or in the
tests, never once in the code under test** -- a mutant that would not compile,
two the Debug tree structurally could not see, three whose real judge is a
CTest entry the harness could not run until decision 139 taught it to, and one
that found no run-time case can require an alignment to be a power of two,
because the suite can only pass ones that are.

**gcc earned its keep twice in M1-12, and the second time it changed a design.**
First it rejected a guard whose only statement was `assert(false)`:
once assertions are live such a function never returns, glibc marks the
failure path so clang can see it, and `-Wmissing-noreturn` said so -- where
three Windows trees had been silent, because MSVC's `assert` does not mark its
own. The attribute would have been a **lie** under `NDEBUG`, where the body is
empty and the function does return, so the fix was to move the assertion to the
call site, where it now names the condition rather than the constant `false`.
Then it rejected the claim that a ratio of two lengths is not a number of
pixels -- and it was **right**: an mp-units *kind* restricts implicit
conversion and permits explicit construction by design, and the agreement of
the other three front ends was an accident of `Scalar<>`'s deleted conversion
operator rather than anything a kind promised. A pixel has a dimension of its
own now (decision 137), and the claim holds on all three implementations by
construction. **One toolchain's disagreement was worth more than three
toolchains' agreement**, which is rule 20's argument in one episode.

*(And before those, on 2026-09-22, it
rejected a nested `std::array` initialiser in `tests/test_camera.cpp` that
clang and MSVC had both accepted -- `-Werror=missing-braces`, three times over
-- after the suite had been green under two front ends for a day. The fix is
the brace pair all three want.)*

On a machine without the fixture,
`test_fixture_file` and `test_sun` each skip a case. Catch2 **v3.16.0**.

**Three implementations now agree to the digit**, which is what `windows-msvc`
was added on 2026-09-14 to find out. It builds the whole tree, renderer
included — the only configuration that does, since both Linux presets are core
only — with **zero warnings under `/Wall /WX /permissive-`**, and passes all 160
CTest entries including the GPU smoke test and ERFA's two validation programs,
which MSVC compiles as C. *(This said 140 until 2026-09-21, which was right
until M1-09 and M1-10 added twenty cases between them -- and this file said 160
eighteen lines further down at the same time. One number, two places, exactly
what the note at the top of this file is about.)*

The seeds for the random sweeps are written into the suites: `20260905` in
`tests/test_orbit_scales.cpp`, `20260910` in `tests/test_time.cpp` and
`20260919` in `tests/test_fixture_file.cpp` and `tests/test_astro_time.cpp`,
`20260920` in `tests/test_math.cpp`, `tests/test_earth_orientation.cpp` and
`tests/test_view_math.cpp`, and `20260921` in `tests/test_projection.cpp`,
each the date its suite was written. **`test_projection` seeds only one of its
sweeps**, and says so: its monotonicity case is a fixed geometric grid of 100
points per decade, which covers the interval rather than sampling it, and only
the three-dimensional field-of-view sweep is a random draw. **`tests/test_sun.cpp` has no seed, and
says so**: its sweeps are exhaustive daily grids over 2000-2050 rather than
random draws, which cover more of the interval than a sample of it would and
reproduce exactly. **`tests/test_render_quality.cpp` has no seed either, and
says so**, on the same argument pushed one step further: whole-number
arithmetic goes wrong at the ends, at the powers of two and at zero, and a
uniform draw over the whole 32-bit range reaches the last few thousand values
with a probability indistinguishable from zero -- so a random sweep would have
been the *weaker* instrument, not merely the less reproducible one. Its probes
are those places, crossed with themselves. `test_time` draws straight from the engine rather
than through a `std::` distribution, whose algorithm the standard leaves to the
library, so its sweep is the same dates under MSVC's library and libstdc++. A
failure prints the failing case's parameters,
through Catch2's `CAPTURE`, which reports them on failure rather than on every
run. Catch2 prints a `Randomness seeded to:` line that differs between runs; it
seeds only `GENERATE` and `--order rand`, neither of which this project uses,
and the sweeps' own generators are still seeded from `kSweepSeed`.
`catch_discover_tests` makes each `TEST_CASE` its own CTest test, so `ctest -R`
selects one and `ctest -N` lists **210**: the 202 Catch2 cases, the two
fixture tests, ERFA's two validation programs, the three count-wraparound
probes and `orbsim_smoke`.
`ctest -LE gpu` lists 209, and `ctest -L fixtures` the two fixture tests.
Both Linux presets are core only and so run 209, and so does a
`-DORBSIM_BUILD_APP=OFF` tree -- **re-measured 2026-09-23**, 424 targets, 198
listed and 198 passing, because that is the structural claim
[ADR 0012](adr/0012-orbsim-view.md) makes and it is worth nothing unless
somebody builds it. M1-09 first ran it, at the 148 entries of that day.
*(The four numbers in this paragraph read 173, 172, 172 and 172 until
2026-09-23, and every one of them had been stale since M1-09 and M1-10 added
twenty cases on 2026-09-21 -- the same day the paragraph eighteen lines above
was corrected from 140 to 160 and this one was not. One fact, several places,
which is what the note at the top of this file is about; they are now read
from `ctest -N` rather than carried.)* The
tree is **deleted after each check rather than kept**: a headless tree that
sits around goes stale, and a stale tree reports green while proving nothing
([`PROJECT_STATE.md`](PROJECT_STATE.md) section 8).

## What this machine has

Recorded because "it builds here" is only useful with the versions attached.
How to install and run any of it is
[`PROJECT_STATE.md`](PROJECT_STATE.md) section 2.

| Tool | Version | Location on this machine |
|---|---|---|
| clang / clang-tidy / clang-format | 23.1.0 | `C:\GitHub\clang+llvm-23.1.0-x86_64-pc-windows-msvc\bin` |
| CMake (the one that works) | 4.3.1, CLion-bundled | `C:\Users\U439644\AppData\Local\Programs\CLion\bin\cmake\win\x64\bin\cmake.exe` |
| CMake (on PATH) | 3.31.2 | `C:\Program Files\CMake\bin` |
| Ninja | 1.12.0 | `C:\Strawberry\c\bin` |
| Vulkan SDK | 1.4.357.0 | `C:\VulkanSDK\1.4.357.0` |
| Python (for the format hook) | 3.14.0 | `C:\Program Files\PyManager` |
| MSVC toolchain | **VS 18 Insiders, MSVC 14.51.36231** | clang targets the MSVC ABI and needs its headers and libs. Upgraded from VS2022 Community 14.44.35207 on 2026-09-13, and the 2022 install is gone: `C:\Program Files\Microsoft Visual Studio\18\Insiders\` is the only one on disk, so clang now emits `-fms-compatibility-version=19.51`. A new standard library is a full rebuild of every tree — the same argument as the clang-upgrade gotcha in [`PROJECT_STATE.md`](PROJECT_STATE.md) section 8 |
| WSL 2 + Ubuntu | 26.04 LTS ("resolute") | Installed 2026-09-07. clang 23.1.1, gcc-14 14.3.0, cmake 4.2.3, ninja 1.13.2. This is where UBSan and the second compiler live |
| GPU in use | NVIDIA RTX A2000 | The renderer requires a discrete GPU where one exists |

**One clang version, everywhere: 23.1.** 23.1.0 on Windows, 23.1.1 in WSL from
`apt.llvm.org` — the same release branch; apt.llvm.org publishes branch builds
rather than the exact tag. `gcc-14` is the second *implementation* and is not
tied to that number.

Pinned dependencies: SDL3, vk-bootstrap, VMA, Vulkan-Headers,
Vulkan-Utility-Libraries, Catch2, mp-units and ERFA — eight, all fetched at a
pinned tag by `CMakeLists.txt`. **mp-units was the first one `orbsim_core`
links**, header-only; **ERFA, since M1-05, is the first it needs at link
time** -- a static C library our build compiles from its sources, so the
headless core still needs nothing installed. Their licences, the ones decided
but not yet pinned, and which files of `bc7enc_rdo` may be compiled, are in
[`THIRD_PARTY.md`](../THIRD_PARTY.md).

## Decision records

**The list is [`adr/README.md`](adr/README.md)**, next to the records
themselves, so that browsing the directory finds it. **Twenty-two are accepted**:
0008 to 0015 written by
[M1-02](plan/tasks/m1-02-record-the-decisions.md) from the decisions taken on
2026-09-08, then 0016 — ERFA computes the astronomy — and 0017 — every warning
as an error — both on 2026-09-11, 0018 — `stateFromElements` reports — on
2026-09-13, 0019 on 2026-09-17, and **0020 — a transform carries the units of
both spaces it maps between — on 2026-09-20 with M1-09**, which extends 0019
from vectors to the matrices that act on them and was accepted rather than
proposed because the spike was built and measured first. **0021 followed it
the same day**, at the owner's request, and supersedes 0020's one clause that
left frames out: a transform now carries the frames it maps between, and the
transpose is typed as the **dual** map it is, which is what makes its
composition law hold for a chain containing a projection. Runtime cost
measured at nil before the design was chosen.

**0022 -- a scalar with a physical bound validates itself -- on 2026-09-22
with M1-87.** `Eccentricity` and `GravParam` hold their own bounds, so
`OrbitError::NonPositiveGravity` and its five checks are gone and three
unreachable assertions with them. It **supersedes 0019's clause** that
`Eccentricity` is an mp-units *kind* -- a class holds a bound where a kind
cannot, and is at least as non-convertible -- and 0019 carries the note back.
`GravParam` is deliberately not superseded: it keeps its quantity, so 0019's
own example, `mu / (r*r)` producing an acceleration type, still holds. That
turned out to be possible only after the alternative had been ruled, and the
cheaper design was put back up rather than absorbed.

**0019 — vectors carry their unit** — landed in two commits, and both of its
steps are done. `core/Units.hpp`'s nine
types are built on mp-units `v2.5.0`, so `Metres / Seconds` is a
`MetresPerSecond` and `Eccentricity` is a *kind* no other ratio converts into;
and `Vec3<R>` is templated on an mp-units **reference**, so `cross(r, v)` is
m²/s — a unit with no name in this codebase and none needed — and
`Position + Velocity` does not compile. It supersedes 0001 on the mechanism and
closes rule 17 of [`VERIFICATION.md`](VERIFICATION.md).

The record's own "What the precondition spike measured" section carries the
numbers, including the one that nearly stopped step 2: mp-units has no
`vector_product` on quantities, in `v2.5.0` or on master. Templating `Vec3` on
the reference rather than on a quantity sidesteps it — the unit algebra is the
library's and the vector operations are ours.

An accepted ADR is immutable, so the numbers inside one are not maintained
here: they are what was true when the decision was taken.
